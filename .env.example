# ------------------------------------------------------------------------------
# Core security
# ------------------------------------------------------------------------------

# Used to sign JWT session cookies
# REQUIRED – app will refuse to start if missing
JWT_SECRET=put-a-long-random-string-here

# Login credentials (PASSWORD must be a SHA-256 hash)
LOGIN_EMAIL=you@example.com
PASSWORD=sha256_hash_of_your_password


# ------------------------------------------------------------------------------
# Runtime language support (1 = enabled, 0 = disabled)
# Controls which interpreters are available for "Run" actions
# ------------------------------------------------------------------------------

RUNTIME_PHP=1
RUNTIME_NODE=0
RUNTIME_PYTHON=0
RUNTIME_GO=0
RUNTIME_LUA=0

# ------------------------------------------------------------------------------
# Hosting / deployment mode
# ------------------------------------------------------------------------------

# local   = filesystem only, no public domains
# online  = public hosting with domains & SSL
HOSTING_MODE=local

# Wildcard domain used for hosted sites (optional)
# Example: magick.host or yourdomain.com
WILDCARD_DOMAIN=

# Where Cute Magick admin is installed
ROOT_DOMAIN=localhost:3000

# Enable HTTPS (requires reverse proxy / valid certs)
SSL_ENABLED=0

# For automatically creating the first site at root domain
AUTO_CREATE_FIRST_SITE=1