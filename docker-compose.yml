services:
  app:
    environment:
      - NODE_ENV=production
    build: .
    env_file:
      - .env
    volumes:
      - sites:/app/sites
      - app-data:/app/data
      - ssh-data:/app/.ssh
volumes:
  ssh-data:
  app-data:
  sites: